server:
  port: 8761

#configuration to make this application doesn't register with itself
eureka:
  client:
    register-with-eureka: false # important to avoid self-registration
    fetch-registry: false # important to avoid fetching registry

spring:
  application:
    name: eureka-server