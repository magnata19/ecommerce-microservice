version: "3.8"

services:
  customers-db:
    image: postgres:15
    container_name: customers-db
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: customers_db
    ports:
      - "5434:5432"
    volumes:
      - customer_data:/var/lib/postgresql/data

volumes:
  customer_data: